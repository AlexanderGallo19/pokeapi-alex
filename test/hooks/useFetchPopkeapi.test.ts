// import { renderHook } from "@testing-library/react"
// import useFetchPopkeapi from "../../src/hooks/useFetchPopkeapi";
// import { type PokemonsResponse } from "../../src/models/pokemon/Pokemons"

describe('Test on file useFetchPopkeapi', () => {

  
  test('should return a list pokemons', () => {
   

    // const { result } = renderHook( () => useFetchPopkeapi<PokemonsResponse>("https://pokeapi.co/api/v2/pokemon?limit=16&offset=0")  );
    // const { hasError, isLoading } = result.current;

    // expect( isLoading ).toBe(true);
    // expect( hasError ).toBe(false);
  })
})
